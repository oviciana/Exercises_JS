/* Define a function called ​isBoolean​ that receives a
 value and return true if the value received is a boolean */
function fnIsBoolean(vValue){
  if(typeof(vValue) === "boolean"){
    return true
  }
  else {
    return false
  }
}